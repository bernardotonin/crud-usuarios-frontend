import { CreatePermissionForm } from "@/components/UserDashboard/CreatePermissionForm";

export default function PermissionCreatePage() {
  return (
    <div className="flex items-center justify-center h-[90vh] font-[family-name:var(--font-geist-sans)]">
      <CreatePermissionForm />
    </div>
  );
}
